// main.js: main script for src/view/index.html.

console.log({'main.js initial': chrome.runtime});

// Comment out the following `let` and `void` => SUCCESS
// Uncomment the `let` or `void` => FAILURE

//let W = window.frames[0];
//void window.frames[0];

console.log({'main.js end': chrome.runtime});
// vi: set ts=4 sts=4 sw=4 et ai fo-=o fo-=r: //
